 
require 'spec_helper'
 

describe Address do

  
  describe '#get_children' do
    it 'works' do
      # parent = double('parent')
      # postcode = double('postcode')
      # result = Address.get_children(parent, postcode)
      # expect(result).not_to be_nil
    end
  end

  
  describe '#decompose_address' do
    it 'works' do
      # location = double('location')
      # params = double('params')
      # result = Address.decompose_address(location, params)
      # expect(result).not_to be_nil
    end
  end

end
