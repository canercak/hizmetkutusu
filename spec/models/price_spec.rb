# -*- encoding: utf-8 -*-

require 'spec_helper'
 
describe Price do
  
  describe '#get_high_lows' do
    it 'works' do
      # category_id = double('category_id')
      # var = double('var')
      # provider = double('provider')
      # result = Price.get_high_lows(category_id, var, provider)
      # expect(result).not_to be_nil
    end
  end

end
